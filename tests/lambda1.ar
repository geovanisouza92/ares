nums = 2..50;
for i in 2..8 do
  nums = filter(def (x) => x == i or x % i, nums);
print(nums);

sentence = "It is raining cats and begings";
words = sentence.split();
words;
lengths = words.map(def (word) => (word.length()));
lengths;