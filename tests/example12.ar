module Fatorial

  method fat(num: Integer): Integer
  begin
    if 0 == num then
      Result = 1;
    else
      Result = num * fat(num - 1);
  end

end

# Fatorial de 6 = 720
assert(720 == fat(6));
