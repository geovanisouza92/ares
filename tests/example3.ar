# inline beginc "Esta é a descrição ou texto de ajuda para este objeto. Tobegins os objetos
# podem ter este informativo embutibegin";
module Clientes

  class Base
    var _id: Integer ;
  end

  # interface IPessoa
  #   def TemBoaReputacao? : Boolean;
  # end

  class Pessoa > Base, IPessoa
    def TemBoaReputacao?() : Boolean
    begin
        # Retorna verdadeiro somente se a pessoa for de boa fé...
        Result = !Nome.IsEmpty?;
    end
    var Nome: String ;
  end

  class PessoaFisica > Pessoa
    var CPF: String ;
  end

  class PessoaJuridica
      > Pessoa
    var CNPJ: String ;
  end

# inline beginc "Lista tobegins os clientes ativos no sistema";
  def ListarTobeginsOsClientes(): Array
  begin
    Result = (from Cliente in DBO.Clientes
               where Cliente.TemBoaReputacao?);
  end

end
