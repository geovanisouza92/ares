nums = 2..50;
for i in 2..8 do
  nums = filter(lambda (x) x == i || x % i, nums);
end
print(nums);

sentence = "It is raining cats and dogs";
words = sentence.split();
words;
lengths = words.map(lambda (word) word.length());
lengths;