module Garage

import System.IO;

  class Base
    method Base() # Construtor
    begin
      # Gerar código automático
    end
    var ID: Integer; # invariants not nil and > 0
  end

  class RentalAgency > Base
    var Address: String;
    var PhoneNo: String;
    var Manager: Pessoa;
  end
  
  class Person > Base
    var Name: String; # invariants not nil
    var Address: String;
  end
  
  class CustomerRecord
    var CustomerID: Integer ;
    var RentalHistory: Strings ;
  private
    method Initialize()
    begin
      # Inicializa Histórico
    end
  end
  
  class Customer > Base, Person
    method Reserve()
    begin
    
    end
    
    method PicksUp()
    begin
    
    end
    
    method Pays()
    begin
    
    end
    
    method Returns()
    begin
    
    end

    var Phone: String ;
    var BirthDate: Date ;
    var LicenseNo: String ; # like \[0-9]+\ # Validabegin com regex
  end
  
  class Car > Base
    var FleetNo: Integer ;
    var Model: String ;
    var RegNo: String ;
    var Condition: String ;
    var Status: String ;
    
    method Reserved?(): Boolean
    begin
    
    end
    
    method InService?(): Boolean
    begin
    
    end
  end
  
  class Employee > Person
    var PhoneNo: String ;
    var DateHired: Date ;
  end
  
  class RentalAgent > Employee
    var CommissionRate: Float ;
    var Tar;
    
    method Calculate()
    begin
    
    end
  end
  
  class Mechanic > Employee
    var Skills: Strings ;
    var Qualifications: String ;
  private
    method Initialize()
    begin
      # Initialize Skills & Qualifications
    end
  end
  
  class ServiceLog > Base
    var ServiceHistory: String ;
    var ProgressHistory: Strings ;
  private
    method Initialize()
    begin
      # Initialize ProgressHistory
    end
  end
  
end
