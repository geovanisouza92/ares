module Fibonacci

  def fib(num: Integer): Integer
  begin
    if 0 == num then
      return 0;
    elif 1 == num then
      return 1;
    else
      Result = fib(num - 1) + fib(num - 2);
  end

end

# Fibonacci até 50...
WriteLn(fib(50));
