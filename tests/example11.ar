
int fib (int num = 0)
{
  var Result = 0;
  if (0 == num)
    Result = 0;
  else if (1 == num)
    Result = 1;
  else
    Result = fib(num - 1) + fib(num - 2);
  return Result;
}

int main()
{
  WriteLn(fib(50));
}
